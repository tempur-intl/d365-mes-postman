{
  "info": {
    "name": "SBOM",
    "description": "Secret BOM (SBOM) – XDS Policy Validation\n\nPurpose:\nVerifies that D365 Extended Data Security (XDS) policies correctly filter items 18000-18499 from all protected data entities.\n\nTest data:\n  Parent item   : 85375  (BOM header – all components visible)\n  Phantom item  : 89002  (phantom BOM – all lines are in the restricted range)\n  Control items : 14244, 18963, 18959  (must remain visible)\n  Restricted range: 18000-18499  (must NOT appear in any response – see restrictedRangeMin/Max)\n\nStructure:\nEach folder maps to one XDS-protected D365 table. Run a folder to validate that table's policy. Every request checks:\n  1. HTTP 200 OK\n  2. No restricted item IDs (18000-18499) appear in any item field\n  3. At least one record is returned (proof that XDS has not '  blocked everything)\n\nVariables required (set in environment):\n  base_url    – e.g. https://your-d365.operations.dynamics.com\n  access_token – OAuth 2.0 bearer token\n  company_id  – legal entity (e.g. 500)",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Authentication",
      "item": [
        {
          "name": "Get OAuth Token",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test(\"Status code is 200\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var jsonData = pm.response.json();",
                  "pm.environment.set(\"access_token\", jsonData.access_token);"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/x-www-form-urlencoded"
              }
            ],
            "body": {
              "mode": "urlencoded",
              "urlencoded": [
                {
                  "key": "grant_type",
                  "value": "client_credentials"
                },
                {
                  "key": "client_id",
                  "value": "{{client_id}}"
                },
                {
                  "key": "client_secret",
                  "value": "{{client_secret}}"
                },
                {
                  "key": "resource",
                  "value": "{{base_url}}"
                }
              ]
            },
            "url": {
              "raw": "https://login.microsoftonline.com/{{tenant_id}}/oauth2/token",
              "protocol": "https",
              "host": [
                "login",
                "microsoftonline",
                "com"
              ],
              "path": [
                "{{tenant_id}}",
                "oauth2",
                "token"
              ]
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "BOM",
      "description": "XDS policy: BOM table\nValidates that BOM and formula line entities do not expose component items 18000-18499.\nPhantom BOM 89002 contains only components in the restricted range – none should be visible after XDS filtering.\n\nDMF-only (not tested here – use Data Management Framework):\n  PmfFormulaLineEntity  (DMF: Formula lines)",
      "item": [
        {
          "name": "BillOfMaterialsLines",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/BillOfMaterialsLines?$filter=(dataAreaId eq '{{company_id}}') and (BOMId eq '{{parentItem}}' or BOMId eq '{{phantomItem}}')&$top=1000",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "BillOfMaterialsLines"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and (BOMId eq '{{parentItem}}' or BOMId eq '{{phantomItem}}')"
                },
                {
                  "key": "$top",
                  "value": "1000"
                }
              ]
            },
            "description": "GET BillOfMaterialsLines\nFilter: BOMId eq '{{parentItem}}' or BOMId eq '{{phantomItem}}'\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – BillOfMaterialsLines",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    var body = pm.response.json();",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = (pm.response.json() || {}).value || [];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "pm.test(\"Records returned – XDS has not blocked all data\", function () {",
                  "    pm.expect(records.length,",
                  "        \"Zero records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "    ).to.be.above(0);",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "BillOfMaterialsLinesV2",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/BillOfMaterialsLinesV2?$filter=(dataAreaId eq '{{company_id}}') and (BOMId eq '{{parentItem}}' or BOMId eq '{{phantomItem}}')&$top=1000",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "BillOfMaterialsLinesV2"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and (BOMId eq '{{parentItem}}' or BOMId eq '{{phantomItem}}')"
                },
                {
                  "key": "$top",
                  "value": "1000"
                }
              ]
            },
            "description": "GET BillOfMaterialsLinesV2\nFilter: BOMId eq '{{parentItem}}' or BOMId eq '{{phantomItem}}'\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – BillOfMaterialsLinesV2",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    var body = pm.response.json();",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = (pm.response.json() || {}).value || [];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "pm.test(\"Records returned – XDS has not blocked all data\", function () {",
                  "    pm.expect(records.length,",
                  "        \"Zero records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "    ).to.be.above(0);",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "BillOfMaterialsLinesV3",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/BillOfMaterialsLinesV3?$filter=(dataAreaId eq '{{company_id}}') and (BOMId eq '{{parentItem}}' or BOMId eq '{{phantomItem}}')&$top=1000",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "BillOfMaterialsLinesV3"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and (BOMId eq '{{parentItem}}' or BOMId eq '{{phantomItem}}')"
                },
                {
                  "key": "$top",
                  "value": "1000"
                }
              ]
            },
            "description": "GET BillOfMaterialsLinesV3\nFilter: BOMId eq '{{parentItem}}' or BOMId eq '{{phantomItem}}'\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – BillOfMaterialsLinesV3",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    var body = pm.response.json();",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = (pm.response.json() || {}).value || [];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "pm.test(\"Records returned – XDS has not blocked all data\", function () {",
                  "    pm.expect(records.length,",
                  "        \"Zero records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "    ).to.be.above(0);",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "KittingKitLines",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/KittingKitLines?$filter=(dataAreaId eq '{{company_id}}') and (BOMId eq '{{parentItem}}' or BOMId eq '{{phantomItem}}')&$top=1000",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "KittingKitLines"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and (BOMId eq '{{parentItem}}' or BOMId eq '{{phantomItem}}')"
                },
                {
                  "key": "$top",
                  "value": "1000"
                }
              ]
            },
            "description": "GET KittingKitLines\nFilter: BOMId eq '{{parentItem}}' or BOMId eq '{{phantomItem}}'\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – KittingKitLines",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Item fields checked: ItemNumber\\n// NOTE: Data for KittingKitLines may legitimately be absent in some environments.",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    var body = pm.response.json();",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = (pm.response.json() || {}).value || [];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Data-existence assertion skipped for this entity",
                  "// (records may legitimately be absent depending on test data scope)",
                  "if (records.length === 0) {",
                  "    console.warn(\"[KittingKitLines] Zero records returned. Ensure test data covers this entity or remove it from the run.\");",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "FormulaLinesV2",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/FormulaLinesV2?$filter=(dataAreaId eq '{{company_id}}') and (FormulaId eq '{{parentItem}}' or FormulaId eq '{{phantomItem}}')&$top=1000",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "FormulaLinesV2"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and (FormulaId eq '{{parentItem}}' or FormulaId eq '{{phantomItem}}')"
                },
                {
                  "key": "$top",
                  "value": "1000"
                }
              ]
            },
            "description": "GET FormulaLinesV2\nFilter: FormulaId eq '{{parentItem}}' or FormulaId eq '{{phantomItem}}'\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – FormulaLinesV2",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Item fields checked: ItemNumber\\n// NOTE: Data for FormulaLinesV2 may legitimately be absent in some environments.",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    var body = pm.response.json();",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = (pm.response.json() || {}).value || [];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Data-existence assertion skipped for this entity",
                  "// (records may legitimately be absent depending on test data scope)",
                  "if (records.length === 0) {",
                  "    console.warn(\"[FormulaLinesV2] Zero records returned. Ensure test data covers this entity or remove it from the run.\");",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "ChgSentProductReleaseLines",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/ChgSentProductReleaseLines?$filter=(dataAreaId eq '{{company_id}}') and (HeaderProductNumber eq '{{parentItem}}' or HeaderProductNumber eq '{{phantomItem}}')&$top=1000",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "ChgSentProductReleaseLines"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and (HeaderProductNumber eq '{{parentItem}}' or HeaderProductNumber eq '{{phantomItem}}')"
                },
                {
                  "key": "$top",
                  "value": "1000"
                }
              ]
            },
            "description": "GET ChgSentProductReleaseLines\nFilter: HeaderProductNumber eq '{{parentItem}}' or HeaderProductNumber eq '{{phantomItem}}'\nItem fields checked: LineProductNumber, CoByProductItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – ChgSentProductReleaseLines",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Item fields checked: LineProductNumber, CoByProductItemNumber\\n// NOTE: Data for ChgSentProductReleaseLines may legitimately be absent in some environments.",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"LineProductNumber\", \"CoByProductItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    var body = pm.response.json();",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = (pm.response.json() || {}).value || [];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Data-existence assertion skipped for this entity",
                  "// (records may legitimately be absent depending on test data scope)",
                  "if (records.length === 0) {",
                  "    console.warn(\"[ChgSentProductReleaseLines] Zero records returned. Ensure test data covers this entity or remove it from the run.\");",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "TSI_MDMIBOMHeaderVersionLines",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/TSI_MDMIBOMHeaderVersionLines?$filter=(dataAreaId eq '{{company_id}}') and (ManufacturedItemNumber eq '{{parentItem}}' or ManufacturedItemNumber eq '{{phantomItem}}')&$top=1000",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "TSI_MDMIBOMHeaderVersionLines"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and (ManufacturedItemNumber eq '{{parentItem}}' or ManufacturedItemNumber eq '{{phantomItem}}')"
                },
                {
                  "key": "$top",
                  "value": "1000"
                }
              ]
            },
            "description": "GET TSI_MDMIBOMHeaderVersionLines\nFilter: ManufacturedItemNumber eq '{{parentItem}}' or ManufacturedItemNumber eq '{{phantomItem}}'\nItem fields checked: ManufacturedItemNumber, ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – TSI_MDMIBOMHeaderVersionLines",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Item fields checked: ManufacturedItemNumber, ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ManufacturedItemNumber\", \"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    var body = pm.response.json();",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = (pm.response.json() || {}).value || [];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "pm.test(\"Records returned – XDS has not blocked all data\", function () {",
                  "    pm.expect(records.length,",
                  "        \"Zero records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "    ).to.be.above(0);",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "InventJournalTrans",
      "description": "XDS policy: InventJournalTrans table\nValidates that inventory journal transaction entities do not expose items 18000-18499.\nQueries journal lines for items in the restricted range. XDS policy should filter out any journal lines for these restricted items.",
      "item": [
        {
          "name": "EDIInventInventoryAdjustmentJournalEntries",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/EDIInventInventoryAdjustmentJournalEntries?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "EDIInventInventoryAdjustmentJournalEntries"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET EDIInventInventoryAdjustmentJournalEntries\nFilter: ItemNumber in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – EDIInventInventoryAdjustmentJournalEntries",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/EDIInventInventoryAdjustmentJournalEntries?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "EDIInventoryTransferJournalLines",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/EDIInventoryTransferJournalLines?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "EDIInventoryTransferJournalLines"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET EDIInventoryTransferJournalLines\nFilter: ItemNumber in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – EDIInventoryTransferJournalLines",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/EDIInventoryTransferJournalLines?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "AssetMaintenanceWorkOrderLineProductJournals",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/AssetMaintenanceWorkOrderLineProductJournals?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ProductNumber}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "AssetMaintenanceWorkOrderLineProductJournals"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ProductNumber}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET AssetMaintenanceWorkOrderLineProductJournals\nFilter: ProductNumber in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ProductNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – AssetMaintenanceWorkOrderLineProductJournals",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ProductNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ProductNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ProductNumber') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/AssetMaintenanceWorkOrderLineProductJournals?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "InventoryAdjustmentJournalEntriesV2",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/InventoryAdjustmentJournalEntriesV2?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "InventoryAdjustmentJournalEntriesV2"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET InventoryAdjustmentJournalEntriesV2\nFilter: ItemNumber in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – InventoryAdjustmentJournalEntriesV2",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/InventoryAdjustmentJournalEntriesV2?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "InventoryCountingJournalLines",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/InventoryCountingJournalLines?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "InventoryCountingJournalLines"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET InventoryCountingJournalLines\nFilter: ItemNumber in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – InventoryCountingJournalLines",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/InventoryCountingJournalLines?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "InventoryMovementJournalEntries",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/InventoryMovementJournalEntries?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "InventoryMovementJournalEntries"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET InventoryMovementJournalEntries\nFilter: ItemNumber in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – InventoryMovementJournalEntries",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/InventoryMovementJournalEntries?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "InventoryMovementJournalEntriesV2",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/InventoryMovementJournalEntriesV2?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "InventoryMovementJournalEntriesV2"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET InventoryMovementJournalEntriesV2\nFilter: ItemNumber in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – InventoryMovementJournalEntriesV2",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/InventoryMovementJournalEntriesV2?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "InventoryMovementJournalEntriesV3",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/InventoryMovementJournalEntriesV3?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "InventoryMovementJournalEntriesV3"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET InventoryMovementJournalEntriesV3\nFilter: ItemNumber in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – InventoryMovementJournalEntriesV3",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/InventoryMovementJournalEntriesV3?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "InventoryMovementJournalEntriesV4",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/InventoryMovementJournalEntriesV4?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "InventoryMovementJournalEntriesV4"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET InventoryMovementJournalEntriesV4\nFilter: ItemNumber in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – InventoryMovementJournalEntriesV4",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/InventoryMovementJournalEntriesV4?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "InventoryTransferJournalEntries",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/InventoryTransferJournalEntries?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "InventoryTransferJournalEntries"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET InventoryTransferJournalEntries\nFilter: ItemNumber in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – InventoryTransferJournalEntries",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/InventoryTransferJournalEntries?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "InventoryServiceAdjustmentJournalEntries",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/InventoryServiceAdjustmentJournalEntries?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "InventoryServiceAdjustmentJournalEntries"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET InventoryServiceAdjustmentJournalEntries\nFilter: ItemNumber in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – InventoryServiceAdjustmentJournalEntries",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/InventoryServiceAdjustmentJournalEntries?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "ProjectItemJournalTrans",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/ProjectItemJournalTrans?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "ProjectItemJournalTrans"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET ProjectItemJournalTrans\nFilter: ItemId in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemId"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – ProjectItemJournalTrans",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ItemId",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemId\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemId') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/ProjectItemJournalTrans?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "InventJournalTransTransTaxInformations",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/InventJournalTransTransTaxInformations?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_TaxInventVATItemId}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "InventJournalTransTransTaxInformations"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_TaxInventVATItemId}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET InventJournalTransTransTaxInformations\nFilter: TaxInventVATItemId in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: TaxInventVATItemId"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – InventJournalTransTransTaxInformations",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: TaxInventVATItemId",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"TaxInventVATItemId\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_TaxInventVATItemId') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/InventJournalTransTransTaxInformations?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "InventTrans",
      "description": "XDS policy: InventTrans table\nValidates that inventory transaction entities do not expose items 18000-18499.\nQueries inventory transactions for items in the restricted range. XDS policy should filter out any transactions for these restricted items.",
      "item": [
        {
          "name": "InventTransCDSEntities",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/InventTransCDSEntities?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "InventTransCDSEntities"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET InventTransCDSEntities\nFilter: ItemId in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemId"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – InventTransCDSEntities",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top=1000 truncation hiding a leak.",
                  "// Item fields checked: ItemId",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemId\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemId') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/InventTransCDSEntities?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "InventTransExtended",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/InventTransExtended?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "InventTransExtended"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET InventTransExtended\nFilter: ItemId in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemId"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – InventTransExtended",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top=1000 truncation hiding a leak.",
                  "// Item fields checked: ItemId",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemId\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemId') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/InventTransExtended?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "AtlasInventTrans",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/AtlasInventTrans?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "AtlasInventTrans"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET AtlasInventTrans\nFilter: ItemId in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemId"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – AtlasInventTrans",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top=1000 truncation hiding a leak.",
                  "// Item fields checked: ItemId",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemId\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemId') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/AtlasInventTrans?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "TSIInventTransCollection",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/TSIInventTransCollection?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "TSIInventTransCollection"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET TSIInventTransCollection\nFilter: ItemId in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemId"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – TSIInventTransCollection",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top=1000 truncation hiding a leak.",
                  "// Item fields checked: ItemId",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemId\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemId') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/TSIInventTransCollection?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "ProdBOM",
      "description": "XDS policy: ProdBOM table\nValidates that production BOM line entities do not expose component items 18000-18499.\nProduction order for item 85375 explodes phantom BOM 89002 into components in the restricted range 18000-18499 – none should appear after XDS.",
      "item": [
        {
          "name": "ProductionOrderBillOfMaterialLines",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/ProductionOrderBillOfMaterialLines?$filter=(dataAreaId eq '{{company_id}}') and ({{filter_ProductionOrderBillOfMaterialLines}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "ProductionOrderBillOfMaterialLines"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{filter_ProductionOrderBillOfMaterialLines}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET ProductionOrderBillOfMaterialLines\nFilter: ProductionOrderNumber eq '{{productionOrderNumber}}' if set (order-scoped, bounded result set), else ItemNumber in {{restrictedItems}} only – in fallback mode the test uses @odata.count plus a chained controlItems-only check instead of relying on $top.\nItem fields checked: ItemNumber, ComponentItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – ProductionOrderBillOfMaterialLines",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// If productionOrderNumber is set, filter is order-scoped (small, bounded result set).",
                  "// Otherwise filter is restrictedItems ONLY; @odata.count (not value.length) avoids $top",
                  "// truncation hiding a leak, and a chained pm.sendRequest proves controlItems still return.",
                  "// Item fields checked: ItemNumber, ComponentItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\", \"ComponentItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "var orderSet = !!(pm.variables.get('productionOrderNumber') || '').trim();",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "if (orderSet) {",
                  "    pm.test(\"Records returned – XDS has not blocked all data\", function () {",
                  "        pm.expect(records.length,",
                  "            \"Zero records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "} else {",
                  "    var totalCount = body[\"@odata.count\"];",
                  "",
                  "    pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "        pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "        pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "    });",
                  "",
                  "    var baseUrl       = pm.variables.get('base_url') || '';",
                  "    var accessToken   = pm.variables.get('access_token') || '';",
                  "    var companyId     = pm.variables.get('company_id') || '';",
                  "    var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "    pm.sendRequest({",
                  "        url: baseUrl + \"/data/ProductionOrderBillOfMaterialLines?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "        method: 'GET',",
                  "        header: {",
                  "            'Authorization': 'Bearer ' + accessToken,",
                  "            'Accept': 'application/json',",
                  "            'OData-Version': '4.0'",
                  "        }",
                  "    }, function (err, res) {",
                  "        var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "        var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "        pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "            pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "            pm.expect(controlCount,",
                  "                \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "            ).to.be.above(0);",
                  "        });",
                  "    });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "BatchOrderFormulaLines",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/BatchOrderFormulaLines?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "BatchOrderFormulaLines"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemNumber}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET BatchOrderFormulaLines\nFilter: ItemNumber in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemNumber, ComponentItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – BatchOrderFormulaLines",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: ItemNumber, ComponentItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\", \"ComponentItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/BatchOrderFormulaLines?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "TSI_ProdBOMLines",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/TSI_ProdBOMLines?$filter=(dataAreaId eq '{{company_id}}') and ({{filter_TSI_ProdBOMLines}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "TSI_ProdBOMLines"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{filter_TSI_ProdBOMLines}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET TSI_ProdBOMLines\nFilter: ProdId eq '{{productionOrderNumber}}' if set (order-scoped, bounded result set), else ItemId in {{restrictedItems}} only – in fallback mode the test uses @odata.count plus a chained controlItems-only check instead of relying on $top.\nItem fields checked: ItemId"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – TSI_ProdBOMLines",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// If productionOrderNumber is set, filter is order-scoped (small, bounded result set).",
                  "// Otherwise filter is restrictedItems ONLY; @odata.count (not value.length) avoids $top",
                  "// truncation hiding a leak, and a chained pm.sendRequest proves controlItems still return.",
                  "// Item fields checked: ItemId",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemId\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "var orderSet = !!(pm.variables.get('productionOrderNumber') || '').trim();",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "if (orderSet) {",
                  "    pm.test(\"Records returned – XDS has not blocked all data\", function () {",
                  "        pm.expect(records.length,",
                  "            \"Zero records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "} else {",
                  "    var totalCount = body[\"@odata.count\"];",
                  "",
                  "    pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "        pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "        pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "    });",
                  "",
                  "    var baseUrl       = pm.variables.get('base_url') || '';",
                  "    var accessToken   = pm.variables.get('access_token') || '';",
                  "    var companyId     = pm.variables.get('company_id') || '';",
                  "    var controlFilter = pm.collectionVariables.get('controlItems_ItemId') || '';",
                  "",
                  "    pm.sendRequest({",
                  "        url: baseUrl + \"/data/TSI_ProdBOMLines?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "        method: 'GET',",
                  "        header: {",
                  "            'Authorization': 'Bearer ' + accessToken,",
                  "            'Accept': 'application/json',",
                  "            'OData-Version': '4.0'",
                  "        }",
                  "    }, function (err, res) {",
                  "        var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "        var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "        pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "            pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "            pm.expect(controlCount,",
                  "                \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "            ).to.be.above(0);",
                  "        });",
                  "    });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "RobExProdBOM",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/RobExProdBOM?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "RobExProdBOM"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET RobExProdBOM\nFilter: ItemId in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemId"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – RobExProdBOM",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top=1000 truncation hiding a leak.",
                  "// Item fields checked: ItemId",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemId\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemId') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/RobExProdBOM?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "ProdJournalBOM",
      "description": "XDS policy: ProdJournalBOM table\nValidates that production picking list journal entities do not expose consumed items 18000-18499.\nPicking list for production order of 85375 should show control components ['14244', '18963', '18959'] but hide all items in the restricted range.",
      "item": [
        {
          "name": "ProductionPickingListJournalEntries",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/ProductionPickingListJournalEntries?$filter=(dataAreaId eq '{{company_id}}') and ({{filter_ProductionPickingListJournalEntries}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "ProductionPickingListJournalEntries"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{filter_ProductionPickingListJournalEntries}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET ProductionPickingListJournalEntries\nFilter: ProductionOrderNumber eq '{{productionOrderNumber}}' if set (order-scoped, bounded result set), else ItemNumber in {{restrictedItems}} only – in fallback mode the test uses @odata.count plus a chained controlItems-only check instead of relying on $top.\nItem fields checked: ItemNumber"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – ProductionPickingListJournalEntries",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// If productionOrderNumber is set, filter is order-scoped (small, bounded result set).",
                  "// Otherwise filter is restrictedItems ONLY; @odata.count (not value.length) avoids $top",
                  "// truncation hiding a leak, and a chained pm.sendRequest proves controlItems still return.",
                  "// Item fields checked: ItemNumber",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemNumber\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "var orderSet = !!(pm.variables.get('productionOrderNumber') || '').trim();",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "if (orderSet) {",
                  "    pm.test(\"Records returned – XDS has not blocked all data\", function () {",
                  "        pm.expect(records.length,",
                  "            \"Zero records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "} else {",
                  "    var totalCount = body[\"@odata.count\"];",
                  "",
                  "    pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "        pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "        pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "    });",
                  "",
                  "    var baseUrl       = pm.variables.get('base_url') || '';",
                  "    var accessToken   = pm.variables.get('access_token') || '';",
                  "    var companyId     = pm.variables.get('company_id') || '';",
                  "    var controlFilter = pm.collectionVariables.get('controlItems_ItemNumber') || '';",
                  "",
                  "    pm.sendRequest({",
                  "        url: baseUrl + \"/data/ProductionPickingListJournalEntries?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "        method: 'GET',",
                  "        header: {",
                  "            'Authorization': 'Bearer ' + accessToken,",
                  "            'Accept': 'application/json',",
                  "            'OData-Version': '4.0'",
                  "        }",
                  "    }, function (err, res) {",
                  "        var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "        var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "        pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "            pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "            pm.expect(controlCount,",
                  "                \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "            ).to.be.above(0);",
                  "        });",
                  "    });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "EDIProdPickingListLines",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/EDIProdPickingListLines?$filter=(dataAreaId eq '{{company_id}}') and ({{filter_EDIProdPickingListLines}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "EDIProdPickingListLines"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{filter_EDIProdPickingListLines}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET EDIProdPickingListLines\nFilter: ProdId eq '{{productionOrderNumber}}' if set (order-scoped, bounded result set), else ItemId in {{restrictedItems}} only – in fallback mode the test uses @odata.count plus a chained controlItems-only check instead of relying on $top.\nItem fields checked: ItemId"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – EDIProdPickingListLines",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// If productionOrderNumber is set, filter is order-scoped (small, bounded result set).",
                  "// Otherwise filter is restrictedItems ONLY; @odata.count (not value.length) avoids $top",
                  "// truncation hiding a leak, and a chained pm.sendRequest proves controlItems still return.",
                  "// Item fields checked: ItemId",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemId\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "var orderSet = !!(pm.variables.get('productionOrderNumber') || '').trim();",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "if (orderSet) {",
                  "    pm.test(\"Records returned – XDS has not blocked all data\", function () {",
                  "        pm.expect(records.length,",
                  "            \"Zero records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "} else {",
                  "    var totalCount = body[\"@odata.count\"];",
                  "",
                  "    pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "        pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "        pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "    });",
                  "",
                  "    var baseUrl       = pm.variables.get('base_url') || '';",
                  "    var accessToken   = pm.variables.get('access_token') || '';",
                  "    var companyId     = pm.variables.get('company_id') || '';",
                  "    var controlFilter = pm.collectionVariables.get('controlItems_ItemId') || '';",
                  "",
                  "    pm.sendRequest({",
                  "        url: baseUrl + \"/data/EDIProdPickingListLines?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "        method: 'GET',",
                  "        header: {",
                  "            'Authorization': 'Bearer ' + accessToken,",
                  "            'Accept': 'application/json',",
                  "            'OData-Version': '4.0'",
                  "        }",
                  "    }, function (err, res) {",
                  "        var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "        var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "        pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "            pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "            pm.expect(controlCount,",
                  "                \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "            ).to.be.above(0);",
                  "        });",
                  "    });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "DemandPlanningPickingListJournalEntries",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/DemandPlanningPickingListJournalEntries?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_TSIItemId}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "DemandPlanningPickingListJournalEntries"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_TSIItemId}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET DemandPlanningPickingListJournalEntries\nFilter: TSIItemId in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: TSIItemId"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – DemandPlanningPickingListJournalEntries",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top truncation hiding a leak.",
                  "// Item fields checked: TSIItemId",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"TSIItemId\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_TSIItemId') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/DemandPlanningPickingListJournalEntries?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "ProdCalcTrans",
      "description": "XDS policy: ProdCalcTrans table\nValidates that production cost calculation transaction entities do not expose items 18000-18499.\nQueries calculation transactions for items in the restricted range. XDS policy should filter out any cost calculation records for these restricted items.",
      "item": [
        {
          "name": "ProdCalcTransBiEntities",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/ProdCalcTransBiEntities?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_Resource}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "ProdCalcTransBiEntities"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_Resource}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET ProdCalcTransBiEntities\nFilter: Resource in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: Resource"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – ProdCalcTransBiEntities",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top=1000 truncation hiding a leak.",
                  "// Item fields checked: Resource",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"Resource\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_Resource') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/ProdCalcTransBiEntities?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "ReqTrans",
      "description": "XDS policy: ReqTrans table\nValidates that net/dynamic requirement entities do not expose items 18000-18499.\nMaster planning for 85375 demand generates requirements for direct components ['14244', '18963', '18959'] and (via phantom explosion) items in the restricted range 18000-18499 – the latter must be hidden by XDS.\n\nNOTE: ReqDynamicPlanProductionOrderScheduleEntity has OData=No and DMF=No in D365 – it cannot be tested via OData or DMF.",
      "item": [
        {
          "name": "RobExReqTrans",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/RobExReqTrans?$filter=(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})&$top=100&$count=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "RobExReqTrans"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "(dataAreaId eq '{{company_id}}') and ({{restrictedItems_ItemId}})"
                },
                {
                  "key": "$top",
                  "value": "100"
                },
                {
                  "key": "$count",
                  "value": "true"
                }
              ]
            },
            "description": "GET RobExReqTrans\nFilter: ItemId in {{restrictedItems}} only; $count=true gives the true total (immune to $top truncation).\nExistence proof (controlItems) is a chained pm.sendRequest.\nItem fields checked: ItemId"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS validation – RobExReqTrans",
                  "// Restricted range and test item lists are driven by collection variables:",
                  "//   restrictedRangeMin, restrictedRangeMax  – define the hidden item ID range",
                  "//   parentItem, phantomItem                 – BOM header items used in filters",
                  "// Filter is restrictedItems ONLY, so any row is inherently a leak;",
                  "// @odata.count (not value.length) avoids $top=1000 truncation hiding a leak.",
                  "// Item fields checked: ItemId",
                  "",
                  "var RESTRICTED_MIN = parseInt(pm.collectionVariables.get(\"restrictedRangeMin\") || \"18000\", 10);",
                  "var RESTRICTED_MAX = parseInt(pm.collectionVariables.get(\"restrictedRangeMax\") || \"18499\", 10);",
                  "var ITEM_FIELDS    = [\"ItemId\"];",
                  "",
                  "function isRestricted(val) {",
                  "    if (val === null || val === undefined || val === \"\") return false;",
                  "    var n = parseInt(String(val).trim(), 10);",
                  "    return !isNaN(n) && n >= RESTRICTED_MIN && n <= RESTRICTED_MAX;",
                  "}",
                  "",
                  "var restrictedRangeLabel = \"(\" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX + \")\";",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "var body = pm.response.json() || {};",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = body.value || [];",
                  "var totalCount = body[\"@odata.count\"];",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" exist (true total via @odata.count)\", function () {",
                  "    pm.expect(totalCount, \"@odata.count missing – is $count=true supported for this entity?\").to.not.be.undefined;",
                  "    pm.expect(totalCount, \"XDS LEAK – \" + totalCount + \" restricted-item record(s) exist (filter is restrictedItems only)\").to.equal(0);",
                  "});",
                  "",
                  "pm.test(\"No restricted items \" + restrictedRangeLabel + \" present in any returned item field\", function () {",
                  "    var violations = [];",
                  "    records.forEach(function (record, idx) {",
                  "        ITEM_FIELDS.forEach(function (field) {",
                  "            var val = record[field];",
                  "            if (isRestricted(val)) {",
                  "                violations.push(",
                  "                    \"Record[\" + idx + \"].\" + field + \" = '\" + val + \"' is in restricted range \" + RESTRICTED_MIN + \"-\" + RESTRICTED_MAX",
                  "                );",
                  "            }",
                  "        });",
                  "    });",
                  "    pm.expect(violations,",
                  "        \"XDS LEAK – restricted items visible! \" + violations.join(\"; \")",
                  "    ).to.be.empty;",
                  "});",
                  "",
                  "// Existence proof scoped ONLY to controlItems, also via @odata.count, so it",
                  "// can't be crowded out by restricted-item volume in the main request.",
                  "var baseUrl       = pm.variables.get('base_url') || '';",
                  "var accessToken   = pm.variables.get('access_token') || '';",
                  "var companyId     = pm.variables.get('company_id') || '';",
                  "var controlFilter = pm.collectionVariables.get('controlItems_ItemId') || '';",
                  "",
                  "pm.sendRequest({",
                  "    url: baseUrl + \"/data/RobExReqTrans?$filter=(dataAreaId eq '\" + companyId + \"') and (\" + controlFilter + \")&$top=1&$count=true\",",
                  "    method: 'GET',",
                  "    header: {",
                  "        'Authorization': 'Bearer ' + accessToken,",
                  "        'Accept': 'application/json',",
                  "        'OData-Version': '4.0'",
                  "    }",
                  "}, function (err, res) {",
                  "    var controlBody  = (!err && res) ? (res.json() || {}) : {};",
                  "    var controlCount = controlBody[\"@odata.count\"];",
                  "",
                  "    pm.test(\"Records returned – XDS has not blocked all data (control items, via @odata.count)\", function () {",
                  "        pm.expect(controlCount, \"@odata.count missing from control-item response\").to.not.be.undefined;",
                  "        pm.expect(controlCount,",
                  "            \"Zero control-item records returned. Verify test data exists and the querying identity can read non-restricted records.\"",
                  "        ).to.be.above(0);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Access Control",
      "description": "Access control audit for TSISBOM XDS policies.\n\nThese requests query SecurityUserRoles to identify users who have been manually excluded from the automatic TSISBOM XDS role assignments.\n\nBackground:\nThe TSISBOM XDS roles are assigned automatically to all users. A user whose assignment has been manually set to Disabled is exempted from the restriction and can see items 18000-18499 in full. This folder helps identify those exemptions.",
      "item": [
        {
          "name": "Users excluded from TSISBOM XDS role assignment",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/SecurityUserRoles?$filter= ( SecurityRoleIdentifier eq 'TSISBOM_BOMXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_BOMCaclTransXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_BOMVersionXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_InventJournalTransXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_InventTransXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_ProdBOMXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_ProdCaclTransXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_ProdJournalBOMXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_ReqTransXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_SysMessageXDSRole' ) and AssignmentMode eq Microsoft.Dynamics.DataEntities.RoleAssignmentMode'Manual' and AssignmentStatus eq Microsoft.Dynamics.DataEntities.RoleAssignmentStatus'Disabled'",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "SecurityUserRoles"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": " ( SecurityRoleIdentifier eq 'TSISBOM_BOMXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_BOMCaclTransXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_BOMVersionXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_InventJournalTransXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_InventTransXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_ProdBOMXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_ProdCaclTransXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_ProdJournalBOMXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_ReqTransXDSRole' or SecurityRoleIdentifier eq 'TSISBOM_SysMessageXDSRole' ) and AssignmentMode eq Microsoft.Dynamics.DataEntities.RoleAssignmentMode'Manual' and AssignmentStatus eq Microsoft.Dynamics.DataEntities.RoleAssignmentStatus'Disabled'"
                }
              ]
            },
            "description": "GET SecurityUserRoles\nReturns users who have been manually excluded (AssignmentMode=Manual, AssignmentStatus=Disabled) from the automatic TSISBOM XDS role assignments.\nThese users bypass the XDS restriction and can see items in the restricted range 18000-18499.\n\nRoles checked:\n  TSISBOM_BOMXDSRole\n  TSISBOM_BOMCaclTransXDSRole\n  TSISBOM_BOMVersionXDSRole\n  TSISBOM_InventJournalTransXDSRole\n  TSISBOM_InventTransXDSRole\n  TSISBOM_ProdBOMXDSRole\n  TSISBOM_ProdCaclTransXDSRole\n  TSISBOM_ProdJournalBOMXDSRole\n  TSISBOM_ReqTransXDSRole\n  TSISBOM_SysMessageXDSRole"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// Access control audit – SecurityUserRoles",
                  "// Returns users whose automatic TSISBOM XDS role assignment has been manually disabled.",
                  "// These users can see items in the restricted range 18000-18499.",
                  "",
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    var body = pm.response.json();",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = (pm.response.json() || {}).value || [];",
                  "",
                  "// Log a summary of every exempted user-role pair to the Postman console.",
                  "if (records.length === 0) {",
                  "    console.log(\"ACCESS CONTROL: No users are currently exempted from TSISBOM XDS role assignments.\");",
                  "} else {",
                  "    console.warn(\"ACCESS CONTROL: \" + records.length + \" user-role exemption(s) found – these users can see restricted items 18000-18499:\");",
                  "    records.forEach(function (r, idx) {",
                  "        console.warn(",
                  "            \"  [\" + (idx + 1) + \"] UserID=\" + (r.UserId || r.NetworkDomain || \"(unknown)\") +",
                  "            \"  Role=\" + (r.SecurityRoleIdentifier || \"(unknown)\") +",
                  "            \"  AssignmentMode=\" + (r.AssignmentMode || \"-\") +",
                  "            \"  AssignmentStatus=\" + (r.AssignmentStatus || \"-\")",
                  "        );",
                  "    });",
                  "}",
                  "",
                  "pm.test(\"ACCESS CONTROL – \" + records.length + \" user(s) exempted from TSISBOM XDS restrictions\", function () {",
                  "    // This test always passes – it surfaces the count in the test results panel.",
                  "    // Review the Postman console for the full list of exempted users.",
                  "    pm.expect(true).to.be.true;",
                  "});",
                  "",
                  "// ---------------------------------------------------------------------------",
                  "// Visualizer – one column per SecurityRoleName, listing UserID values.",
                  "// ---------------------------------------------------------------------------",
                  "(function buildVisualizer() {",
                  "    // Group by SecurityRoleName, collect UserID per group.",
                  "    var byRole = {};",
                  "    records.forEach(function (rec) {",
                  "        var roleName = rec.SecurityRoleName || rec.SecurityRoleIdentifier || '(unknown)';",
                  "        if (!byRole[roleName]) { byRole[roleName] = []; }",
                  "        byRole[roleName].push(rec.UserId || '(unknown)');",
                  "    });",
                  "",
                  "    var roleNames = Object.keys(byRole).sort();",
                  "    roleNames.forEach(function (name) { byRole[name].sort(); });",
                  "",
                  "    var template = `",
                  "    <html>",
                  "    <head>",
                  "    <style>",
                  "      body  { font-family: sans-serif; font-size: 13px; margin: 12px; background: #f9f9f9; }",
                  "      h2    { margin: 0 0 12px; font-size: 15px; color: #333; }",
                  "      p.sub { margin: -8px 0 14px; font-size: 11px; color: #777; }",
                  "      .grid { display: flex; flex-wrap: wrap; gap: 10px; }",
                  "      .col  { background: #fff; border: 1px solid #ddd; border-radius: 6px;",
                  "              padding: 10px 14px; min-width: 180px; flex: 1 1 180px; }",
                  "      .col h3 { margin: 0 0 8px; font-size: 13px; color: #333;",
                  "                border-bottom: 1px solid #eee; padding-bottom: 6px; }",
                  "      ul    { margin: 0; padding: 0 0 0 16px; }",
                  "      li    { margin: 4px 0; color: #333; }",
                  "    </style>",
                  "    </head>",
                  "    <body>",
                  "      <h2>TSISBOM XDS Role Exemptions</h2>",
                  "      <p class='sub'>Users with AssignmentMode=Manual, AssignmentStatus=Disabled — these users can see items 18000–18499</p>",
                  "      {{#if roles.length}}",
                  "      <div class='grid'>",
                  "        {{#each roles}}",
                  "        <div class='col'>",
                  "          <h3>{{name}} ({{users.length}})</h3>",
                  "          <ul>{{#each users}}<li>{{this}}</li>{{/each}}</ul>",
                  "        </div>",
                  "        {{/each}}",
                  "      </div>",
                  "      {{else}}",
                  "      <p style='color:#27ae60;font-weight:bold;'>No exemptions found – all users are subject to XDS restrictions.</p>",
                  "      {{/if}}",
                  "    </body>",
                  "    </html>`,",
                  "",
                  "    vizData = {",
                  "        roles: roleNames.map(function (name) {",
                  "            return { name: name, users: byRole[name] };",
                  "        })",
                  "    };",
                  "",
                  "    pm.visualizer.set(template, vizData);",
                  "}());"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "System administrators",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/SecurityUserRoles?$filter= SecurityRoleIdentifier eq '-SYSADMIN-'",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "SecurityUserRoles"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": " SecurityRoleIdentifier eq '-SYSADMIN-'"
                }
              ]
            },
            "description": "GET SecurityUserRoles\nReturns all users assigned to the System Administrator role (-SYSADMIN-).\nSystem administrators bypass all XDS policies and can see items in the restricted range 18000-18499."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test(\"Status 200 OK\", function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test(\"Response is valid OData JSON with value array\", function () {",
                  "    var body = pm.response.json();",
                  "    pm.expect(body).to.have.property(\"value\").that.is.an(\"array\");",
                  "});",
                  "",
                  "var records = (pm.response.json() || {}).value || [];",
                  "",
                  "var roleName = (records[0] && (records[0].SecurityRoleName || records[0].SecurityRoleIdentifier)) || '-SYSADMIN-';",
                  "",
                  "var userIds = records",
                  "    .map(function (r) { return r.UserId || '(unknown)'; })",
                  "    .sort();",
                  "",
                  "if (userIds.length === 0) {",
                  "    console.log(\"SYSADMIN: No users found with the System Administrator role.\");",
                  "} else {",
                  "    console.warn(\"SYSADMIN: \" + userIds.length + \" system administrator(s) – these users bypass all XDS restrictions:\");",
                  "    userIds.forEach(function (id, i) { console.warn(\"  [\" + (i + 1) + \"] \" + id); });",
                  "}",
                  "",
                  "pm.test(\"SYSADMIN – \" + userIds.length + \" system administrator(s) found\", function () {",
                  "    pm.expect(true).to.be.true;",
                  "});",
                  "",
                  "// ---------------------------------------------------------------------------",
                  "// Visualizer – sorted list of system administrator UserId values.",
                  "// ---------------------------------------------------------------------------",
                  "var template = `",
                  "<html>",
                  "<head>",
                  "<style>",
                  "  body { font-family: sans-serif; font-size: 13px; margin: 12px; background: #f9f9f9; }",
                  "  h2   { margin: 0 0 6px; font-size: 15px; color: #333; }",
                  "  p.sub { margin: 0 0 14px; font-size: 11px; color: #777; }",
                  "  .card { background: #fff; border: 1px solid #ddd; border-radius: 6px;",
                  "          padding: 10px 14px; display: inline-block; min-width: 220px; }",
                  "  .card h3 { margin: 0 0 8px; font-size: 13px; color: #333;",
                  "             border-bottom: 1px solid #eee; padding-bottom: 6px; }",
                  "  ul   { margin: 0; padding: 0 0 0 16px; }",
                  "  li   { margin: 4px 0; color: #333; }",
                  "  .empty { color: #27ae60; font-weight: bold; }",
                  "</style>",
                  "</head>",
                  "<body>",
                  "  <h2>System Administrators</h2>",
                  "  <p class='sub'>Users assigned to -SYSADMIN- — these users bypass all XDS restrictions and can see items 18000–18499</p>",
                  "  {{#if users.length}}",
                  "  <div class='card'>",
                  "    <h3>{{roleName}} ({{users.length}})</h3>",
                  "    <ul>{{#each users}}<li>{{this}}</li>{{/each}}</ul>",
                  "  </div>",
                  "  {{else}}",
                  "  <p class='empty'>No system administrators found.</p>",
                  "  {{/if}}",
                  "</body>",
                  "</html>`;",
                  "",
                  "pm.visualizer.set(template, { roleName: roleName, users: userIds });"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Users with missing TSISBOM XDS role assignments",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{access_token}}"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "OData-Version",
                "value": "4.0"
              }
            ],
            "url": {
              "raw": "{{base_url}}/data/SystemUsers?$filter=Enabled eq true&$select=UserID,UserName&$top=5000",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "data",
                "SystemUsers"
              ],
              "query": [
                {
                  "key": "$filter",
                  "value": "Enabled eq true"
                },
                {
                  "key": "$select",
                  "value": "UserID,UserName"
                },
                {
                  "key": "$top",
                  "value": "5000"
                }
              ]
            },
            "description": "GET SystemUsers (Enabled eq true)\nSecondary call via pm.sendRequest fetches SecurityUserRoles for all 10 TSISBOM XDS roles (any assignment status).\nUsers present in SystemUsers but absent from SecurityUserRoles have no role entry at all – the automatic role assignment never ran for those accounts.\nUsers present but with fewer than 10 roles have a partial assignment failure.\n\nRoles checked:\n  TSISBOM_BOMXDSRole\n  TSISBOM_BOMCaclTransXDSRole\n  TSISBOM_BOMVersionXDSRole\n  TSISBOM_InventJournalTransXDSRole\n  TSISBOM_InventTransXDSRole\n  TSISBOM_ProdBOMXDSRole\n  TSISBOM_ProdCaclTransXDSRole\n  TSISBOM_ProdJournalBOMXDSRole\n  TSISBOM_ReqTransXDSRole\n  TSISBOM_SysMessageXDSRole"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "// XDS check – Users with missing TSISBOM XDS role assignments",
                  "// Main request: GET SystemUsers (Enabled eq true).",
                  "// One pm.sendRequest per role; each fetches only users assigned that role,",
                  "// then computes the diff against all active users.",
                  "",
                  "var ROLES = [",
                  "    'TSISBOM_BOMXDSRole',",
                  "    'TSISBOM_BOMCaclTransXDSRole',",
                  "    'TSISBOM_BOMVersionXDSRole',",
                  "    'TSISBOM_InventJournalTransXDSRole',",
                  "    'TSISBOM_InventTransXDSRole',",
                  "    'TSISBOM_ProdBOMXDSRole',",
                  "    'TSISBOM_ProdCaclTransXDSRole',",
                  "    'TSISBOM_ProdJournalBOMXDSRole',",
                  "    'TSISBOM_ReqTransXDSRole',",
                  "    'TSISBOM_SysMessageXDSRole'",
                  "];",
                  "",
                  "pm.test('Status 200 OK', function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test('Response is valid OData JSON with value array', function () {",
                  "    var body = pm.response.json();",
                  "    pm.expect(body).to.have.property('value').that.is.an('array');",
                  "});",
                  "",
                  "var EXCLUDED_USERS = {",
                  "    'archiveserviceapp': true, 'dataintserviceuser': true, 'datasyncframeworkapp': true,",
                  "    'dataversesearchapp': true, 'demandplanappuser': true, 'dualwriteserviceuser': true,",
                  "    'dynamicsmaintappuser': true, 'frserviceuser': true, 'financialreportsapp': true,",
                  "    'iapapp': true, 'monitoringappuser': true, 'powerplatformapp': true,",
                  "    'retailserviceaccount': true, 'scaleunitmanagement': true, 'scaleunitpipeline': true,",
                  "    'syshealthserviceuser': true",
                  "};",
                  "var allUsers = (pm.response.json() || {}).value || [];",
                  "var users    = allUsers.filter(function (u) {",
                  "    return !EXCLUDED_USERS[(u.UserID || '').toLowerCase()];",
                  "});",
                  "var baseUrl = pm.environment.get('base_url') || '';",
                  "var token   = pm.environment.get('access_token') || '';",
                  "",
                  "var byRole  = {};",
                  "var pending = ROLES.length;",
                  "",
                  "ROLES.forEach(function (role) {",
                  "    byRole[role] = [];",
                  "",
                  "    pm.sendRequest({",
                  "        url:    baseUrl + '/data/SecurityUserRoles?$filter=SecurityRoleIdentifier eq \\'' + role + '\\'&$select=UserId&$top=5000',",
                  "        method: 'GET',",
                  "        header: {",
                  "            'Authorization': 'Bearer ' + token,",
                  "            'Accept':        'application/json',",
                  "            'OData-Version': '4.0'",
                  "        }",
                  "    }, function (err, res) {",
                  "        var records = (!err && res ? (res.json() || {}).value : null) || [];",
                  "",
                  "        var assignedUids = {};",
                  "        records.forEach(function (r) {",
                  "            assignedUids[(r.UserId || '').toLowerCase()] = true;",
                  "        });",
                  "",
                  "        users.forEach(function (u) {",
                  "            var uid = (u.UserID || '').toLowerCase();",
                  "            if (uid && !assignedUids[uid]) {",
                  "                byRole[role].push(u.UserID);",
                  "            }",
                  "        });",
                  "",
                  "        pending--;",
                  "        if (pending > 0) { return; }",
                  "",
                  "        // All 10 roles done – build summary and visualizer",
                  "        var gapCount = 0;",
                  "        ROLES.forEach(function (r) { gapCount += byRole[r].length; });",
                  "",
                  "        pm.test('ACCESS CONTROL \\u2013 ' + gapCount + ' role assignment gap(s) found', function () {",
                  "            pm.expect(true).to.be.true;",
                  "        });",
                  "",
                  "        var rolesWithGaps = ROLES",
                  "            .map(function (r) { return { name: r, users: byRole[r].sort() }; })",
                  "            .filter(function (r) { return r.users.length > 0; });",
                  "",
                  "        var template = `",
                  "    <html><head><style>",
                  "      body  { font-family: sans-serif; font-size: 13px; margin: 12px; background: #f9f9f9; }",
                  "      h2    { margin: 0 0 12px; font-size: 15px; color: #333; }",
                  "      p.sub { margin: -8px 0 14px; font-size: 11px; color: #777; }",
                  "      .grid { display: flex; flex-wrap: wrap; gap: 10px; }",
                  "      .col  { background: #fff; border: 1px solid #ddd; border-radius: 6px; padding: 10px 14px; min-width: 180px; flex: 1 1 180px; }",
                  "      .col h3 { margin: 0 0 8px; font-size: 13px; color: #333; border-bottom: 1px solid #eee; padding-bottom: 6px; }",
                  "      ul { margin: 0; padding: 0 0 0 16px; }",
                  "      li { margin: 4px 0; color: #333; }",
                  "    </style></head><body>",
                  "      <h2>TSISBOM XDS Role Assignment Gaps</h2>",
                  "      <p class='sub'>Active users missing a SecurityUserRoles entry for the listed role</p>",
                  "      {{#if roles.length}}",
                  "      <div class='grid'>",
                  "        {{#each roles}}<div class='col'>",
                  "          <h3>{{name}} ({{users.length}})</h3>",
                  "          <ul>{{#each users}}<li>{{this}}</li>{{/each}}</ul>",
                  "        </div>{{/each}}",
                  "      </div>",
                  "      {{else}}",
                  "      <p style='color:#27ae60;font-weight:bold;'>No gaps found \\u2013 all active users have all 10 TSISBOM XDS roles assigned.</p>",
                  "      {{/if}}",
                  "    </body></html>`,",
                  "",
                  "        vizData = { roles: rolesWithGaps };",
                  "",
                  "        pm.visualizer.set(template, vizData);",
                  "    });",
                  "});"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    }
  ],
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "// Build OData eq/or filter fragments from restrictedItems and controlItems for each field name variant.",
          "// Update restrictedItems/controlItems variables to change which item numbers are queried.",
          "function parseList(varName) {",
          "    return (pm.collectionVariables.get(varName) || '')",
          "        .split(',')",
          "        .map(function(s) { return s.trim(); })",
          "        .filter(Boolean);",
          "}",
          "",
          "var restrictedItems = parseList('restrictedItems');",
          "var controlItems    = parseList('controlItems');",
          "",
          "function buildFilter(items, field) {",
          "    return items.map(function(id) { return field + \" eq '\" + id + \"'\"; }).join(' or ');",
          "}",
          "",
          "var FIELDS = ['ItemNumber', 'ItemId', 'ProductNumber', 'TaxInventVATItemId', 'TSIItemId', 'Resource', 'ComponentItemNumber'];",
          "FIELDS.forEach(function(field) {",
          "    pm.collectionVariables.set('restrictedItems_' + field, buildFilter(restrictedItems, field));",
          "    pm.collectionVariables.set('controlItems_' + field,    buildFilter(controlItems, field));",
          "});",
          "",
          "// If productionOrderNumber is set, filter production entities by that order.",
          "// Otherwise fall back to a restrictedItems-only filter (like InventTrans); the request's",
          "// test script then uses $count=true + a chained controlItems-only pm.sendRequest so",
          "// $top truncation can't hide a leak or crowd out the existence proof.",
          "function buildOrderOrItemFilter(orderField, itemField) {",
          "    // pm.variables.get (not collectionVariables) so an env-level override/clear is honored.",
          "    var orderVal = (pm.variables.get('productionOrderNumber') || '').trim();",
          "    if (orderVal) {",
          "        return orderField + \" eq '\" + orderVal + \"'\";",
          "    }",
          "    return buildFilter(restrictedItems, itemField);",
          "}",
          "",
          "pm.collectionVariables.set('filter_ProductionOrderBillOfMaterialLines', buildOrderOrItemFilter('ProductionOrderNumber', 'ItemNumber'));",
          "pm.collectionVariables.set('filter_TSI_ProdBOMLines',                   buildOrderOrItemFilter('ProdId', 'ItemId'));",
          "pm.collectionVariables.set('filter_ProductionPickingListJournalEntries', buildOrderOrItemFilter('ProductionOrderNumber', 'ItemNumber'));",
          "pm.collectionVariables.set('filter_EDIProdPickingListLines',            buildOrderOrItemFilter('ProdId', 'ItemId'));"
        ]
      }
    }
  ],
  "variable": [
    {
      "key": "parentItem",
      "value": "85375",
      "type": "string"
    },
    {
      "key": "phantomItem",
      "value": "89002",
      "type": "string"
    },
    {
      "key": "controlItems",
      "value": "14244,18963,18959",
      "type": "string"
    },
    {
      "key": "restrictedRangeMin",
      "value": "18000",
      "type": "string"
    },
    {
      "key": "restrictedRangeMax",
      "value": "18499",
      "type": "string"
    },
    {
      "key": "restrictedItems",
      "value": "18009,18011,18014,18015",
      "type": "string",
      "description": "Comma-separated list of specific restricted item numbers present in test data. Used as OData eq/or filter on entities where ge/le is not supported on the item field."
    },
    {
      "key": "restrictedItems_ItemNumber",
      "value": "",
      "type": "string",
      "description": "Auto-built by collection pre-request script from restrictedItems. OData or-clause for ItemNumber field."
    },
    {
      "key": "restrictedItems_ItemId",
      "value": "",
      "type": "string",
      "description": "Auto-built by collection pre-request script from restrictedItems. OData or-clause for ItemId field."
    },
    {
      "key": "restrictedItems_ProductNumber",
      "value": "",
      "type": "string",
      "description": "Auto-built by collection pre-request script from restrictedItems. OData or-clause for ProductNumber field."
    },
    {
      "key": "restrictedItems_TaxInventVATItemId",
      "value": "",
      "type": "string",
      "description": "Auto-built by collection pre-request script from restrictedItems. OData or-clause for TaxInventVATItemId field."
    },
    {
      "key": "productionOrderNumber",
      "value": "",
      "type": "string",
      "description": "Optional production order number from test data, used to filter ProdBOM/ProdJournalBOM requests. Left blank by default since no fixed order number is guaranteed to exist across environments; set it to target a specific order, or leave blank to fall back to filtering by restrictedItems/controlItems."
    },
    {
      "key": "restrictedItems_TSIItemId",
      "value": "",
      "type": "string",
      "description": "Auto-built by collection pre-request script from restrictedItems. OData or-clause for TSIItemId field."
    },
    {
      "key": "restrictedItems_Resource",
      "value": "",
      "type": "string",
      "description": "Auto-built by collection pre-request script from restrictedItems. OData or-clause for Resource field."
    },
    {
      "key": "controlItems_ItemNumber",
      "value": "",
      "type": "string",
      "description": "Auto-built by collection pre-request script from controlItems. OData or-clause for ItemNumber field."
    },
    {
      "key": "controlItems_ItemId",
      "value": "",
      "type": "string",
      "description": "Auto-built by collection pre-request script from controlItems. OData or-clause for ItemId field."
    },
    {
      "key": "controlItems_ProductNumber",
      "value": "",
      "type": "string",
      "description": "Auto-built by collection pre-request script from controlItems. OData or-clause for ProductNumber field."
    },
    {
      "key": "controlItems_TaxInventVATItemId",
      "value": "",
      "type": "string",
      "description": "Auto-built by collection pre-request script from controlItems. OData or-clause for TaxInventVATItemId field."
    },
    {
      "key": "controlItems_TSIItemId",
      "value": "",
      "type": "string",
      "description": "Auto-built by collection pre-request script from controlItems. OData or-clause for TSIItemId field."
    },
    {
      "key": "controlItems_Resource",
      "value": "",
      "type": "string",
      "description": "Auto-built by collection pre-request script from controlItems. OData or-clause for Resource field."
    }
  ]
}